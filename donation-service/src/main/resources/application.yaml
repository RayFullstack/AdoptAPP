spring:
  application:
    name: donation-service

  datasource:
    url: jdbc:h2:mem:donationdb
    driverClassName: org.h2.Driver
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: update

  h2:
    console:
      enabled: true
      path: /h2-console

server:
  port: 8085